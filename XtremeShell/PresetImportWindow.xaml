<Window x:Class="XtremeShell.PresetImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Preset"
        Height="660"
        Width="860"
        Background="#1f070f"
        FontFamily="pack://application:,,,/Fonts/#Poppins"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <Style x:Key="ActionButton" TargetType="{x:Type Button}">
            <Setter Property="FontFamily" Value="pack://application:,,,/Fonts/#Poppins"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#4A2E3A"/>
            <Setter Property="BorderBrush" Value="#6B4C57"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#5A3E4A"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#8B6F7F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#B82A5A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#332530"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#4A3A43"/>
                                <Setter Property="Foreground" Value="#777777"/>
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#8B2635"/>
            <Setter Property="BorderBrush" Value="#A53D4A"/>
            <Setter Property="MinWidth" Value="170"/>
        </Style>

        <Style x:Key="PresetCheckBox" TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="pack://application:,,,/Fonts/#Poppins"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="CheckBorder"
                                    Background="#1A0A0F"
                                    BorderBrush="#6B4C57"
                                    BorderThickness="2"
                                    CornerRadius="5"
                                    Width="18"
                                    Height="18"
                                    Margin="0,0,11,0"
                                    VerticalAlignment="Center">
                                <Path x:Name="CheckMark"
                                      Data="M3,8 L7,12 L14,4"
                                      Stroke="#FFFFFF"
                                      StrokeThickness="2"
                                      Visibility="Collapsed"
                                      StrokeLineJoin="Round"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"/>
                            </Border>
                            <ContentPresenter Grid.Column="1"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Stretch"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background" Value="#B82A5A"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#FF8B6F7F"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#FF8B6F7F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="XtremeScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="ThumbBorder" Background="#6B4C57" CornerRadius="8" Margin="2"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#8B6F7F"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#B82A5A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="XtremeScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="12"/>
            <Setter Property="MinWidth" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="Transparent">
                            <Border Background="#2A1219" CornerRadius="8" Margin="2"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True" Focusable="False">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource XtremeScrollBarThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#3D1A29"
                BorderBrush="#4A2E3A"
                BorderThickness="1"
                CornerRadius="10"
                Padding="18"
                Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Import Preset"
                           Foreground="White"
                           FontSize="26"
                           FontWeight="Bold"
                           x:Name="PresetSummary"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Background="#1A0A0F"
                BorderBrush="#4A2E3A"
                BorderThickness="1"
                CornerRadius="10"
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ScrollViewer.Resources>
                    <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource XtremeScrollBar}"/>
                </ScrollViewer.Resources>
                <ItemsControl ItemsSource="{Binding Groups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,12">
                                <TextBlock Text="{Binding Name}"
                                           Foreground="White"
                                           FontSize="17"
                                           FontWeight="Bold"
                                           Margin="4,0,0,8"/>
                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#2A1219"
                                                    BorderBrush="#4A2E3A"
                                                    BorderThickness="1"
                                                    CornerRadius="7"
                                                    Padding="10,8"
                                                    Margin="0,0,0,6">
                                                <CheckBox Style="{StaticResource PresetCheckBox}"
                                                          IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{Binding Name}"
                                                                   Foreground="White"
                                                                   FontSize="14"
                                                                   FontWeight="SemiBold"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                    </Grid>
                                                </CheckBox>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="2"
                x:Name="ProgressLogPanel"
                Background="#2A1219"
                BorderBrush="#4A2E3A"
                BorderThickness="1"
                CornerRadius="8"
                Padding="10"
                Margin="0,10,0,0"
                Visibility="Collapsed">
            <TextBox x:Name="ProgressLog"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="#D9B9C7"
                     FontSize="12"
                     Height="72"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Disabled"/>
        </Border>

        <Border Grid.Row="3"
                Background="#3D1A29"
                BorderBrush="#4A2E3A"
                BorderThickness="1"
                CornerRadius="10"
                Padding="12"
                Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="StatusText"
                           Text="Ready"
                           Foreground="#D9B9C7"
                           FontSize="13"
                           VerticalAlignment="Center"/>

                <Button
                        Content="Close"
                        Style="{StaticResource ActionButton}"
                        Click="CloseButton_Click" Grid.ColumnSpan="3" Margin="466,5,5,5"/>

                <Button x:Name="ApplyButton"
                        Grid.Column="3"
                        Content="Apply Preset"
                        Style="{StaticResource PrimaryButton}"
                        Click="ApplyButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
